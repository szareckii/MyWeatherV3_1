<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/listDaysItemLayout"
    android:layout_width="match_parent"
    android:layout_height="44dp"

    android:orientation="horizontal">

    <TextView
        android:id="@+id/itemCityNameTextView"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:background="?attr/selectableItemBackground"
        android:textSize="@dimen/sizeTextItem"
        style="@style/Padding"
        android:gravity="start"
        tools:text="@string/itemTextInRV" />

    <TextView
        android:id="@+id/itemDateHistoryTextView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground"
        android:textSize="@dimen/sizeTextItem"
        style="@style/Padding"
        android:gravity="start"
        tools:text="@string/itemTextInRV" />

    <TextView
        android:id="@+id/itemTemperTextView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="?attr/selectableItemBackground"
        android:textSize="@dimen/sizeTextItem"
        style="@style/Padding"
        android:gravity="start"
        tools:text="@string/itemTextInRV" />

</LinearLayout>